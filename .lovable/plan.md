
The user wants the learner payment flow to include a "Add/Update payment method" screen like Stripe's billing portal screenshot — for first-time users and for users who want to change their card. Currently the project uses **PayFast** (per memory + edge functions: `payfast-create-payment`, `payfast-charge-token`, `payfast-itn`), not Stripe.

Let me explore the current payment flow before planning.
